
DataSet dsJobDetails = adapterJobEntry.GetData(jobNum);                       
                           
DataRow dr = adapterJobEntry.JobEntryData.JobHead.Rows[0];

dr.BeginEdit(); 
dr["FieldName_c"] = NewJobNumber; 
dr.EndEdit(); 

if (adapterJobEntry.Update()) 
{ 
      EpiMessageBox.Show("Successfully updated");
}
